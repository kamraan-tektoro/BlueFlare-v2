{
  "name": "blueflare-api",
  "version": "1.0.0",
  "description": "Azure Functions backend for BlueFlare contact form",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@azure/data-tables": "^13.2.2",
    "@azure/functions": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "azure-functions-core-tools": "^4.0.5571",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}

